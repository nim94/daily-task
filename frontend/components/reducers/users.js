/* const ADD_USER = 'ADD_USER';

export const addUser = (name, mail, psw) => ({
    type: ADD_USER,
    name,
    mail,
    psw,
})

export const { user } = (state={}, action) => {
    switch(action.type) {
        case ADD_USER:
            return action.name, 
            action.mail, 
            action.psw
        default:
            return state;
    }
} */